{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Launch Website in Chrome",
      "type": "chrome",
      "request": "launch",
      "url": "http://localhost:8000",
      "webRoot": "${workspaceFolder}",
      "preLaunchTask": "Start Local Website Server"
    },
    {
      "name": "Launch Website in Edge",
      "type": "msedge",
      "request": "launch",
      "url": "http://localhost:8000",
      "webRoot": "${workspaceFolder}",
      "preLaunchTask": "Start Local Website Server"
    }
  ]
}
